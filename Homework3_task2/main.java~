import java.util.Arrays;
import java.util.Scanner;
import javax.swing.JOptionPane;

/*
 * 2* На её основе необходимо сделать программу для конвертации трёх валют (например доллар, евро, рубль)
 *  в трёх разных банках (например, курс приватБанка, ОщадБанка, и банка ПУМБ).
 *   Возможные курсы валют и имена банков должны быть заданы константами. 
 *   Название банка и валюты для конвертации вводятся в консоли.
 */
public class main {

	/**
	 * @param args
	 */

	static Bank[] banks = new Bank[3];

	public static void main(String[] args) {
		Bank bank = new Bank();
		bank.initBanks();
		bank.CheckBank();
		bank.CheckCourse();


	}		
	
}
